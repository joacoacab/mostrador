# order-core/frontend

Frontend del Order Core: Next.js + TypeScript + Tailwind, PWA (`next-pwa`).
Rutas principales: `/panel` (gestión, requiere login), `/pantalla` (solo lectura, tablet/TV del local).

Todavía sin código (ver `docs/tasks.md`, tarea 15 en adelante).
